import SideBarNav from "../Admin/SideBar";
import SideBar from "../Admin/SideBar";

const User = (props) => {
  return (
    <div>
      <SideBarNav></SideBarNav>
    </div>
  );
};

export default User;
